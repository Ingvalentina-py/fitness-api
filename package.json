{
  "name": "fitness-api",
  "version": "0.1.0",
  "private": true,
  "description": "API REST de App Fitness (Express + MongoDB)",
  "license": "UNLICENSED",
  "type": "module",
  "main": "src/app.js",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "lint": "oxlint"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "helmet": "^8.3.0",
    "mongoose": "^9.10.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "oxlint": "^1.83.0"
  }
}
